# Contributing

Use pnpm with Node 24.

1. Install with `pnpm install`.
2. Keep changes scoped.
3. Run targeted tests while working.
4. Run `pnpm check` before requesting review.

Architecture changes require an ADR in `docs/adr`.
