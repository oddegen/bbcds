# Benchmarking

No product benchmark exists yet.

Future benchmark reports must include app version, model version, device/browser metadata, backend, video metadata, sample counts, timings, long tasks, optional memory, and the final decision. Do not claim performance without recorded device, browser, backend, codec, resolution, duration, and sample count.
